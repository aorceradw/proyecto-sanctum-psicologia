{
  "buildCommand": "npm run install:all && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "outputDirectory": "client/dist",
  "env": {
    "VITE_API_URL": {
      "description": "URL de la API (usada en frontend)",
      "required": false
    }
  }
}
